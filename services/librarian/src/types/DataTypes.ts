export interface DataBundle {
    id: string;
    data: any;
    timestamp: string;
}

export interface WorkProduct {
    id: string;
    agentId: string;
    stepId: string;
    data: any;
    timestamp: string;
}