import React from 'react';
import CareerCoachAssistantPage from './CareerCoachAssistantPage';

const CareerAssistant: React.FC<{ clientId: string }> = ({ clientId }) => {
  return (
    <CareerCoachAssistantPage clientId={clientId} />
  );
};

export default CareerAssistant;

