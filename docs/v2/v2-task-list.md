# V2 Task List

This document tracks the high-level tasks for the v2 initiative.

-   **Status:** `pending`, `in_progress`, `completed`

| Task                                             | Layer | Status    | Notes                                                              |
| ------------------------------------------------ | ----- | --------- | ------------------------------------------------------------------ |
| Define and ratify the Assistant SDK API          | L2    | `pending` | The foundational step for the new architecture.                    |
| Build prototype of the Assistant SDK             | L2    | `pending` | Implement the core `Assistant`, `Tool`, and `Conversation` classes. |
| Choose and specify the first vertical assistant  | L3    | `pending` | Leaning towards "Product Manager Assistant".                       |
| Build UI components for the chosen vertical      | L3    | `pending` | Within `mcsreact`.                                                 |
| Develop specialized tools for the chosen vertical | L3    | `pending` | e.g., `JiraTool`, `ConfluenceTool`.                                |
| Integrate SDK with the Core Orchestration Engine | L1/L2 | `pending` | Connect the SDK to the existing plan execution backend.            |
| End-to-end testing of the first vertical         | L1-L3 | `pending` | Ensure the full stack works as intended.                           |
| Stabilize and simplify the Core Engine           | L1    | `pending` | Ongoing effort to reduce complexity and improve reliability.       |

